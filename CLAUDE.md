# share リポジトリ

複数のプロジェクトが同居しているリポジトリです。作業対象のディレクトリを確認してから着手してください。

| ディレクトリ | 内容 |
| --- | --- |
| `agent/content-generator-api/` | コンテンツ生成の開始/中断/再開 API（FastAPI）。**開発中** |
| `agent/proposal-sample/` | 顧客提案ジェネレーター（ADK + FastAPI） |
| `lineage/`, `diff_html/` | 別件の資料 |

## 作業を始める前に

**`agent/content-generator-api/` を触る場合は、何をするより先に
[`agent/content-generator-api/HANDOVER.md`](agent/content-generator-api/HANDOVER.md) を最後まで読むこと。**

そこに、現在地・設計判断とその理由・次にやること・落とし穴がまとまっています。
文脈を持たない状態で読み始める前提で書かれているので、これを読めば作業を再開できます。

**そして作業を終える前に、同じ文書を更新してからコミットすること**（更新ルールは文書の 10 章）。

## 共通の約束

- **日本語**でやりとりする。コード内のコメント・docstring も日本語で書く。
- 依頼者は Python に詳しくない。前提知識を置かず、用語をその場で定義して説明する。
- 抽象的な設計論より、**動く成果物と実際の実行結果**を示す。
- 作業ブランチ以外に push しない。**PR は明示的に求められたときだけ**作る。
